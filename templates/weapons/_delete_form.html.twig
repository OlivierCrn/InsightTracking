<form method="post" action="{{ path('app_weapons_delete', {'id': weapon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weapon.id) }}">
    <button class="btn w-100 btn-danger btn-custom"> Supprimer </button>
</form>
